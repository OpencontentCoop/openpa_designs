<div class="widget">
    <div class="widget_content">
        <h3>Tags</h3>
        {eztagscloud(
        hash(
        'parent_node_id', 2,
        'sort_by', array(
        'keyword', true()
        )
        )
        )}
    </div>
</div>